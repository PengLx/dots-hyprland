# Allow PipeWire to follow common HiRes sample rates so the DAC clock locks
# to the stream's native rate (bit-perfect, no resampling).
context.properties = {
    default.clock.rate          = 48000
    default.clock.allowed-rates = [ 44100 48000 88200 96000 176400 192000 ]
}
